import styled from 'styled-components';

const Districts = () => {
    return (
        <Container>
            
        </Container>
    )
}

export default Districts

const Container = styled.div``;
